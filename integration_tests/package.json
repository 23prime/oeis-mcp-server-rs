{
  "scripts": {
    "test": "vitest run",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.23.0",
    "prettier": "^3.7.1",
    "vitest": "^4.0.15"
  }
}

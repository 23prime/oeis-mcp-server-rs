{
  "scripts": {
    "test": "vitest run",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "prettier": "^3.8.0",
    "vitest": "^4.0.17"
  }
}
